# Python
__pycache__/
*.py[cod]
*.so
build/
dist/
*.egg-info/

# Virtual environments
.venv/

# Tooling caches
.pytest_cache/
.ruff_cache/

# Environment
.env

# Everything under data/ is a build artifact — raw items, extraction
# records, Scrapy resume state, and the built database. All rebuilt by
# re-running the pipeline; the code creates the directories it needs.
# Only the README is tracked.
data/*
!data/README.md

# Claude
.claude/
