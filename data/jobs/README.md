# This directory holds Scrapy JOBDIR state used to resume interrupted
# crawls. Its contents are gitignored (see top-level .gitignore); only
# this README is tracked so the directory exists in fresh clones.
#
# To wipe state and re-crawl from scratch, delete this directory's
# contents (but keep the README) and re-run the spider.
